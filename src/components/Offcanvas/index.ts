import Container from './Container'
import Header from './Header'
import Body from './Body'
import Title from './Title'

const Offcanvas = Object.assign(Container, {
  Header,
  Body,
  Title,
})

export default Offcanvas
